package ru.front.frame;

import javax.swing.*;

public class NumberCardFrame extends JFrame {
    public NumberCardFrame() {
        setTitle("Number Card");
        setSize(600, 300);
        setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
    }
}
